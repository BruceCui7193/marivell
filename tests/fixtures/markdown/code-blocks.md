行内代码：`$5 and $10` 应该保持原样。

围栏代码：

```latex
\begin{equation}
E = mc^2
\end{equation}
```

缩进代码：

    $x^2 + y^2 = z^2$
    \draw (0,0) -- (1,1);

嵌套标记：

    `$a$` and $b$

Mermaid:

```mermaid
graph LR
  A-->B
```
