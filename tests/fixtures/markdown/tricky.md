价格是 $5 and $10 only。

转义美元：\$5，不会变成公式。

字面 token：@@MARKDOWN_EDITOR_MATH_0@@ 应该原样保留。

中文 + 公式混合：状态 $X_0,X_1,\cdots$ 与约束 $s_r = x_q - x_d$。

CRLF 段落
另一行

嵌套列表：
- 外层
  - 内层
- 外层二

脚注[^1]

[^1]: 脚注内容。

图片 ![图](./image.png)
